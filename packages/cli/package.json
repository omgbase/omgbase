{
  "name": "omgbase",
  "version": "0.0.0",
  "private": true,
  "description": "Open Markdown Graph Base — CLI",
  "type": "module",
  "bin": {
    "omgbase": "./dist/src/main.js",
    "omg": "./dist/src/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "@omgbase/core": "workspace:*",
    "yaml": "^2.9.0"
  }
}
