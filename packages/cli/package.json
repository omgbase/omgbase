{
  "name": "omgbase",
  "version": "0.0.0",
  "private": true,
  "description": "Open Markdown Graph Base — CLI",
  "type": "module",
  "bin": {
    "omgbase": "./dist/src/bin.js",
    "omg": "./dist/src/bin.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests yet'",
    "lint": "echo 'no lint yet'"
  },
  "dependencies": {
    "@omgbase/core": "workspace:*"
  }
}
